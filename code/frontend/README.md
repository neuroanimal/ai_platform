# Frontend

This directory contains frontend-side application code.

Scope:
- user-facing interfaces
- visualization and interaction layers
- channel-specific implementations

Channels:
- web: browser-based applications
- mobile: mobile applications
- desktop: desktop applications

Rules:
- frontend may depend on common
- frontend must not depend on backend
- channel-specific code must not contain business logic

Frontend code focuses on presentation and user interaction.
